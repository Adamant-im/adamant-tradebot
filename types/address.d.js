'use strict';

/**
 * Deposit address provided by an exchange object definition.
 * @typedef {Object} ResultItem
 * @prop {string} address Address provided by an exchange.
 * @prop {string} memo Address memo.
 * @prop {string} network Network on which address is available.
 * @typedef {Array<ResultItem>} Result
 */

module.exports = {};
