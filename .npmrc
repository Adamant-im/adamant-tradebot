# Babel 8 + Jest 30: babel-preset-current-node-syntax still pins @babel/plugin-syntax-*@7
# with peer @babel/core ^7; Babel 8 is compatible at runtime but fails strict peer resolution.
legacy-peer-deps=true
